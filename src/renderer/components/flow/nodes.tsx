/**
 * Barrel re-export — all node types, contexts, constants, and defaults.
 * This file exists only for backward-compatible imports (`import from './nodes'`).
 * Add new exports to `./nodes/index.ts`, not here.
 */
export * from './nodes/index'
